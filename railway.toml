[build]
# Dockerfile (no nixpacks): el módulo de placas necesita Chrome + Xvfb.
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# El startCommand lo provee el ENTRYPOINT del Dockerfile (arranca Xvfb + uvicorn).
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
